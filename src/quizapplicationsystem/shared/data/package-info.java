/**
 * Repositories and the shared data store. A single source of truth so a
 * quiz created by a teacher, an account made by an admin, and a result
 * submitted by a student all read and write the same underlying data.
 */
package quizapplicationsystem.shared.data;
