/**
 * Domain entities shared across the admin, teacher, and student areas
 * (User, Teacher, Student, Course, Category, Quiz, Question, QuizResult).
 * These are UI-agnostic so every role can reuse the same data.
 */
package quizapplicationsystem.shared.model;
